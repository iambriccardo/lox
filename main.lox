fun sayHi(outer, inner) {
  for (var i = 0; i < outer; i = i + 1) {
        for (var j = 0; j < inner; j = j + 1) {
            if (j >= 5) {
                break;
            }

            print(j);
        }
    }

    return outer * inner;
}

var result = sayHi(2, 20);
print(result);
